# Railway 部署配置
[phases.setup]
nixPkgs = ["nodejs", "openssl"]

[phases.install]
cmds = ["cd 摇签筒/server && npm install"]

[phases.build]
cmds = ["cd 摇签筒/server && npx prisma generate && npm run build"]

[start]
cmd = "cd 摇签筒/server && npx prisma db push && node dist/index.js"
