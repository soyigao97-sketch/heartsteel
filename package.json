{
  "name": "heartsteel",
  "scripts": {
    "build": "cd 摇签筒/server && npm install && npx prisma generate && npm run build",
    "start": "cd 摇签筒/server && npx prisma db push && node dist/index.js"
  }
}
